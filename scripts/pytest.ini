[pytest]
addopts = -ra --strict-markers --strict-config --tb=short -p no:django
markers =
	property: Hypothesis property-based tests that verify invariants across generated inputs
	e2e: end-to-end tests that drive real git merges in scratch repositories
